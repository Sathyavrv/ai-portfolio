{
  "name": "ai-portfolio",
  "version": "1.0.0",
  "description": "AI-powered portfolio with Gemini integration",
  "main": "index.html",
  "scripts": {
    "dev": "npx vercel dev",
    "build": "echo 'No build step required'",
    "start": "npx vercel dev"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 